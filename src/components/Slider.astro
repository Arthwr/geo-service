---
import slide1 from "../assets/geodesySlide.webp";
import slide2 from "../assets/geologySlide.webp";
import slide3 from "../assets/laserSlide.webp";
---

<!-- Embla Carousel API & npm package
https://www.embla-carousel.com/api/options/
-->
<div class="embla">
  <div class="embla__container">
    <div id="slide-1" class="embla__slide">
      <img src={slide1.src} alt="Изображение инженерных работ по геодезии" />
    </div>
    <div id="slide-2" class="embla__slide">
      <img src={slide2.src} alt="Изображение инженерных работ по геологии" />
    </div>
    <div id="slide-3" class="embla__slide">
      <img src={slide3.src} alt="Изображение работ по лазерному сканированию" />
    </div>
  </div>
  <div class="embla__dots"></div>
</div>

<style is:global>
  .embla {
    margin-top: 30px;
    margin-bottom: 100px;
    overflow: hidden;
    position: relative;
    z-index: 3;
  }
  .embla__container {
    display: flex;
  }
  .embla__slide {
    display: flex;
    justify-content: center;
    flex: 0 0 100%;
    min-width: 0;
  }
  .embla__dots {
    z-index: 5;
    top: 0.2rem;
    position: absolute;
    left: 0;
    right: 0;
    display: flex;
    justify-content: center;
    align-items: center;
  }
  .embla__dot {
    width: 2.4rem;
    height: 2.4rem;
    display: flex;
    align-items: center;
    margin-right: 0.75rem;
    margin-left: 0.75rem;
  }
  .embla__dot:after {
    background: rgba(226, 215, 215, 1);
    border-radius: 0.2rem;
    width: 100%;
    height: 0.3rem;
    content: "";
  }
  .embla__dot--selected:after {
    background: linear-gradient(45deg, rgb(138, 180, 248), rgb(193, 168, 226));
  }
</style>

<script>
  import EmblaCarousel from "embla-carousel";
  import { addDotBtnsAndClickHandlers } from "../scripts/dot-buttons";
  import Autoplay from "embla-carousel-autoplay";

  const emblaNode = document.querySelector(".embla") as HTMLElement;
  const options = {
    loop: true,
    delay: 10000,
    stopOnMouseEnter: true,
    stopOnInteraction: false,
    duration: 30,
  };

  const dotsNode = document.querySelector(".embla__dots");
  const emblaApi = EmblaCarousel(emblaNode, options, [Autoplay()]);
  const removeDotBtnsAndClickHandlers = addDotBtnsAndClickHandlers(
    emblaApi,
    dotsNode
  );

  emblaApi.on("destroy", removeDotBtnsAndClickHandlers);
  console.log(emblaApi.slideNodes()); // Access API
</script>
